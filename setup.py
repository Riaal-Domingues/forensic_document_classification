from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The project is in partial fullfilment of MIT 807.  The aim of the project is to build a classifier that is based on the requirements of a forensic investigation company.  The document classifier needs to classify huge amounts of documents into topics in aid of investigations.',
    author='Riaal Domingues',
    license='BSD-3',
)
